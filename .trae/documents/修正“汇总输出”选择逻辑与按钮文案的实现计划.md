## 选择逻辑修正
- UI：保留“选择所有学院（去除演示账户）”与逐个学院复选框，互不联动。
- 逻辑：
  - 若“选择所有学院（去除演示账户）”勾选，则忽略手动复选框，`selected_codes = 所有学院（排除 demo/显示名含“演示”）`
  - 若未勾选“选择所有学院”，则根据手动复选框生成 `selected_codes`；不回退到“全部”，必须至少选择一个学院，否则提示。
- 代码位置：app.py 的“汇总输出”分支中选择区域（当前含 `selected_names` + 逐项 `checkbox` 与 `select_all`）

## 按钮文案与导出内容
- 将下方下载按钮统一为：
  - “下载所选择学院的问答对 (CSV)” → 合并 `selected_codes` 的所有问答对
  - “下载所选择学院的习题库 (CSV)” → 合并 `selected_codes` 的所有习题
  - “下载所选择学院的研究生习题库 (CSV)” → 合并 `selected_codes` 的习题中 `level == 研究生`
- 文件命名：
  - 多学院：`所选_问答对.csv`、`所选_习题库.csv`、`所选_研究生_习题库.csv`
  - 单学院：`<学院显示名>_qa.csv`、`<学院显示名>_exercises.csv`、`<学院显示名>_研究生_习题库.csv`
- 保留 Excel 导出逻辑（若选择 Excel 格式），但按钮文案仍按“所选择学院”描述；Excel 内容按当前实现组织。
- 代码位置：app.py “汇总输出”中单选与多选分支的下载按钮区域

## 排除演示账户
- 在“选择所有学院”逻辑中，排除 code == "demo" 与显示名包含“演示”的学院。
- 逐项复选框默认不勾选 demo，且勾选时也不影响“选择所有学院”的逻辑（独立）。

## 空选择与提示
- 当既未勾选“选择所有学院”，又未勾选任何学院时，提示“请至少选择一个学院”并禁用下载按钮。

## 测试与验证
- 单选学院 → 三个下载按钮均导出该学院数据
- 多选学院 → 按合并所选导出
- 勾选“选择所有学院” → 正确排除演示账户
- 未选择任何学院且未勾选“选择所有” → 显示提示并不导出

确认后，我将在 app.py 的“汇总输出”模块中完成上述逻辑与按钮文案修改，并进行本地验证。